# Ops Log (2026-02-22)

Date: 2026-02-22  
Target: `https://www.oingapp.com`  
Scope: Post-deploy cron/auth secret stabilization

## 1) Hosting Confirmation
- Verified production host is Railway (`server: railway-edge`), not Vercel.
- Operational secret changes were applied on Railway service `englishapp` in `production`.

## 2) CRON Secret Stabilization
- Synced `CRON_SECRET` across:
- Railway runtime environment (`production/englishapp`)
- GitHub Actions repository secret (`inharlans/englishapp`)
- Triggered workflow: `Scheduled Internal Cron Jobs`
- Run URL: `https://github.com/inharlans/englishapp/actions/runs/22282268763`
- Result: `success`
- Steps confirmed:
- `Validate secrets` success
- `Run plan-expire cron` success
- `Run wordbook-rank cron` success

## 3) Production Endpoint Verification
- `POST /api/internal/cron/plan-expire` with valid bearer secret -> `200`
- `POST /api/internal/cron/wordbook-rank` with valid bearer secret -> `200`

## 4) AUTH_BOOTSTRAP_TOKEN Rotation
- Rotated `AUTH_BOOTSTRAP_TOKEN` in:
- Local runtime file (`.env`)
- Railway runtime environment (`production/englishapp`)
- Validation results after deploy:
- `POST /api/auth/bootstrap` with invalid token -> `403`
- `POST /api/auth/bootstrap` with new token -> `409` (`Bootstrap already completed.`), confirming auth path is valid and bootstrap is idempotent.

## 5) Current State
- Cron auth mismatch issue resolved.
- Scheduled cron workflow restored and passing.
- Bootstrap token rotation completed and verified.
