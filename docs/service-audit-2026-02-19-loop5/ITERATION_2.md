# 2회차 점검 보고서 (2026-02-19)

## 실제 점검 동선
- 오프라인(`/offline`) 반복 사용
- 마켓(`/wordbooks/market`) 목록 재확인

## 제안 5건
1. 오프라인 목록 정렬 기준이 고정되어 대량 저장 시 탐색 속도가 떨어진다.
2. 검색어를 지우기 위해 전체 선택/삭제를 반복해야 한다.
3. 단어 수 기준 정렬이 없어 복습량이 많은 단어장을 우선 고르기 어렵다.
4. 필터/정렬 조합 상태가 UI에서 즉시 드러나지 않는다.
5. 오프라인 삭제 전, 사용자에게 어떤 항목이 먼저 보여질지(정렬 기준) 제어권이 필요하다.

## 이번 회차 반영
- [완료] `app/offline/page.tsx`
  - 정렬 드롭다운(최신순/오래된순/단어 수 많은순) 추가
  - 검색 초기화 버튼 추가
  - 검색 + 정렬 조합을 한 번에 처리하도록 리스트 파이프라인 정리

## 다음 회차 예정
- 마켓 필터 범위를 확장(제작자 이메일 검색)
- 결과 수가 많은 마켓에서 빠른 범주 필터(단어 수 구간) 도입
