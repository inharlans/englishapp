# 2회차 점검 보고서 (2026-02-21)

## 점검 범위
- 홈/단어장/마켓/오프라인 사용자 동선
- 결제 영역 제외

## 실제 점검 동선
1. Railway에서 `englishapp` 배포 상태 확인 (`Online` 확인 후 시작)
2. `/wordbooks` 점검
3. `/wordbooks/market` 점검
4. `/offline` 점검

## 발견 이슈 (5건)
1. [중간] 상단 네비게이션 활성 상태가 하위 경로에서 정확하지 않음
2. [낮음] 상단 네비게이션이 `div role=list` 기반이라 시맨틱이 약함
3. [낮음] 마켓 검색 플레이스홀더가 실제 검색 범위(제작자 이메일 포함)를 반영하지 못함
4. [중간] 다운로드일/동기화일이 UTC 기반 표기(`toISOString`)라 로컬 날짜와 어긋날 수 있음
5. [낮음] `블랙리스트` 용어가 다른 화면의 `차단` 용어와 불일치

## 반영 작업
1. `components/AppNav.tsx`
   - 경로 prefix 기반 활성화(`aria-current`) 처리
   - `ul/li` 시맨틱 구조로 정리
2. `app/wordbooks/market/page.tsx`
   - 검색 플레이스홀더를 `제목, 설명, 제작자 이메일`로 수정
   - 보조 안내 문구 추가
   - 언어 방향 표기 `->`를 `→`로 정리
3. `app/wordbooks/page.tsx`
   - 날짜 표기를 `Asia/Seoul` 기준 로컬 포맷으로 변경
   - `블랙리스트`를 `차단 목록`으로 변경

## 검증 결과
- typecheck: pending
- 테스트: pending
- 배포 확인(Railway): pending

## 다음 회차 우선순위
1. 마켓 카드 액션 버튼 밀집도/모바일 터치성 점검
2. 오프라인 목록 초기 로딩 UX 점검
