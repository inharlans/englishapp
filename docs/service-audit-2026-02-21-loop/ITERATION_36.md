# ITERATION 36 (2026-02-21)

## 범위
- 카드 외 영역 확장 점검/개선: `/wordbooks/[id]/quiz-*`

## 변경 체크리스트 (10개)
- [x] 파트 전환 시 안내 메시지 추가(`n파트로 이동했습니다`)
- [x] 초기 렌더에서는 파트 이동 안내 메시지 스킵(ref guard)
- [x] `Home` 키 경계 처리(첫 파트 안내)
- [x] `End` 키 경계 처리(마지막 파트 안내)
- [x] 파트 요약에 `남은 파트` 표시 추가
- [x] 파트 요약에 `현재 파트 남은 문제` 표시 추가
- [x] `처음` 버튼 경계 처리 메시지 보강
- [x] `이전/다음/마지막` 버튼 경계 처리 메시지 보강
- [x] 파트 점프 입력 onBlur 정규화(clamp)
- [x] 파트 이동/경계 UX를 키보드·버튼 모두 동일 정책으로 통일

## 구현 파일
- `app/wordbooks/[id]/quiz/quizClient.tsx`

## 검증
- `npm run typecheck` 통과
- `npm run lint` 통과
